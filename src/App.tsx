import React from 'react';
import './App.scss';
import AppContext from './models/context';

function App() {
    

    return (
        <AppContext.Provider value={{}}>
            App works!
        </AppContext.Provider>
    );
}

export default App;
